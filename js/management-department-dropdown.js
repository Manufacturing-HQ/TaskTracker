"use strict";

/**
 * Retired Phase 1 legacy shim.
 *
 * Department options now come from the Employee Master/Departments tables via
 * management-employee-qa-access.js. This file intentionally does nothing so
 * older Management HTML cannot replace Department IDs with hard-coded text.
 */
(() => {})();
