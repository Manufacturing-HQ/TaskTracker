"use strict";

/**
 * Retired Phase 1 legacy shim.
 *
 * Employee and item editing are owned by management-master-data.js, with the
 * Employee Master access controls added by management-employee-qa-access.js.
 * This file intentionally does nothing so older cached Management HTML that
 * still references it cannot replace the authoritative Edit buttons/modals.
 */
(() => {})();
